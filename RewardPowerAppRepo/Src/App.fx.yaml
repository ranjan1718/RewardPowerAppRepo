App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(varCurrUserData,User());
        Set(varPopRejectHR,false);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

